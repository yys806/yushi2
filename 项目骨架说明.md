# 珅玉定制 - 完整项目骨架

## 目录结构

- `frontend`（当前根目录 React + Vite）
  - `src/` 前端代码
  - `images/` 热门玉石轮播图资源
- `backend/`（Express + TypeScript）
  - `src/server.ts` 后端骨架入口
  - `config/config.template.json` API key 配置模板
  - `prisma/schema.prisma` 数据库模型草案
- `android/`（Kotlin + Compose）
  - `app/src/main/java/com/yushi/app/` App 骨架
  - `network/` Retrofit API 定义
  - `repository/` 数据层骨架
  - `ui/` ViewModel 骨架

## 运行前端

```bash
npm install
npm run dev
```

## 运行后端

```bash
cd backend
npm install
copy config\config.template.json config\config.local.json
npm run start:dev
```

## Android 打开方式

- 使用 Android Studio 打开 `android/` 目录。
- Gradle Sync 后即可运行。

## 关键说明

- 硅基流动 API key 不写死在代码中，请写入：
  - `backend/config/config.local.json`
- Android 端只请求你的后端，不直接暴露模型 API key。
