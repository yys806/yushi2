# 珅玉定制 Android APK 项目实施计划

## 0. 目标与范围

### 最终目标
- 交付可正常安装到 Android 手机的 `.apk` 文件。
- 支持用户注册/登录。
- 支持「生图」功能（模型：`Qwen/Qwen-Image`）。
- 支持「生成设计灵感与寓意」功能（文本模型：`Pro/MiniMaxAI/MiniMax-M2.5`）。
- API Key 不写死，放在本地配置文件中，由你自行填写。

### 本期范围（MVP）
- Android 客户端（Kotlin + Jetpack Compose）。
- 后端服务（建议：Node.js + NestJS，或你熟悉的等价框架）。
- 数据库（PostgreSQL）存用户与作品记录。
- 硅基流动 API 接入（文本 + 生图）。

---

## 1. 总体架构

### 架构原则
- **密钥只在服务端**使用：Android 不直接调用硅基流动，避免 API Key 泄露。
- Android 只调用自建后端；后端调用硅基流动模型。
- 所有核心能力（注册、生图、灵感、寓意、历史）都走统一后端接口。

### 架构分层
- **Android App**
  - UI：Jetpack Compose
  - 网络：Retrofit + OkHttp
  - 本地：DataStore（保存 token）
- **Backend API**
  - 认证：JWT
  - 业务：用户、作品、生成任务
  - 外部调用：SiliconFlow API
- **Database**
  - 用户表、作品表、生成日志表

---

## 2. 技术选型

### Android
- Kotlin
- Jetpack Compose
- Architecture：MVVM
- DI：Hilt（可选但推荐）
- Networking：Retrofit + OkHttp + Kotlin Serialization 或 Moshi

### Backend
- Node.js 20+
- NestJS
- PostgreSQL + Prisma（或 TypeORM）
- JWT + bcrypt

### 部署
- 后端部署到你的服务器（Docker Compose 推荐）
- Nginx 反向代理 + HTTPS（Let’s Encrypt）

---

## 3. 功能拆解

### A. 账号系统
- 注册：邮箱 + 密码 + 昵称
- 登录：邮箱 + 密码
- 鉴权：JWT（Access Token）
- 个人信息：昵称、邮箱、剩余额度

### B. 生成能力
- 输入：材质、纹饰、成品类型、预算、主体描述
- 文本生成（灵感/寓意）：`Pro/MiniMaxAI/MiniMax-M2.5`
- 图片生成：`Qwen/Qwen-Image`
- 输出：作品名称、灵感、寓意、图像 URL、创建时间

### C. 数据管理
- 历史记录列表（分页）
- 历史详情
- 收藏/取消收藏
- 收藏列表

### D. 额度机制（建议）
- 新用户赠送固定免费次数（例如 5 次）
- 每次生图消耗 1 次
- 后续可接充值（本期可预留接口）

---

## 4. 数据库设计（MVP）

### users
- id (uuid)
- email (unique)
- password_hash
- nickname
- quota_total
- quota_used
- created_at

### works
- id (uuid)
- user_id (fk)
- material
- pattern
- product_type
- budget
- subject
- title
- inspiration_text
- meaning_text
- image_url
- is_favorite (bool, default false)
- created_at

### generation_logs（可选）
- id
- user_id
- model_text
- model_image
- prompt_text
- status
- error_message
- created_at

---

## 5. 后端 API 设计

## 认证
- `POST /auth/register`
- `POST /auth/login`
- `GET /auth/me`

## 生成
- `POST /generate/work`
  - 入参：材质/纹饰/成品/预算/主体
  - 流程：
    1) 校验额度
    2) 调文本模型生成灵感与寓意
    3) 组装生图 prompt
    4) 调图像模型生成图片
    5) 入库并扣减额度
  - 出参：作品完整信息

## 作品管理
- `GET /works/history?page=1&pageSize=20`
- `GET /works/:id`
- `POST /works/:id/favorite`
- `DELETE /works/:id/favorite`
- `GET /works/favorites`

---

## 6. SiliconFlow 接入方案

### 6.1 文本模型（灵感与寓意）
- 模型：`Pro/MiniMaxAI/MiniMax-M2.5`
- 用法：Chat Completions

### 6.2 生图模型
- 模型：`Qwen/Qwen-Image`
- 用法：Images 或对应生成接口（按硅基流动文档实际字段）

### 6.3 你给的示例请求（保留）
```bash
curl --request POST \
  --url https://api.siliconflow.cn/v1/chat/completions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -d '{
    "model": "Pro/zai-org/GLM-4.7",
    "messages": [
      {"role": "system", "content": "你是一个有用的助手"},
      {"role": "user", "content": "你好，请介绍一下你自己"}
    ]
  }'
```

> 说明：上面是你提供的参考。项目里会替换为 `Pro/MiniMaxAI/MiniMax-M2.5` 与 `Qwen/Qwen-Image`。

---

## 7. 配置文件方案（API Key 你自行填写）

### 后端配置文件
- 路径建议：`backend/config/config.local.json`（不提交到 git）

### 模板文件（提交到仓库）
- 路径：`backend/config/config.template.json`

```json
{
  "server": {
    "port": 8080,
    "jwtSecret": "PLEASE_REPLACE"
  },
  "database": {
    "url": "postgresql://user:password@127.0.0.1:5432/shenyu"
  },
  "siliconflow": {
    "baseUrl": "https://api.siliconflow.cn/v1",
    "apiKey": "YOUR_API_KEY",
    "textModel": "Pro/MiniMaxAI/MiniMax-M2.5",
    "imageModel": "Qwen/Qwen-Image"
  }
}
```

### 安全要求
- `config.local.json` 加入 `.gitignore`
- 生产环境优先使用环境变量覆盖

---

## 8. Android 页面与流程

### 页面
- 启动页（可选）
- 登录页 / 注册页
- 首页
- 定制页（输入参数）
- 生成中页（loading）
- 成品页（图 + 灵感 + 寓意）
- 历史页（列表 + 详情）
- 收藏页（列表 + 详情）
- 我的页（账号与额度）

### 关键流程
1. 用户登录成功 -> 保存 token
2. 定制输入 -> 调 `POST /generate/work`
3. 返回作品 -> 跳转成品页
4. 收藏按钮 -> 收藏接口
5. 历史/收藏 -> 列表与详情查询

---

## 9. APK 产物与签名

### Debug APK（联调）
- `./gradlew assembleDebug`
- 产物：`app/build/outputs/apk/debug/app-debug.apk`

### Release APK（可分发安装）
- 创建 keystore
- 配置 `signingConfigs.release`
- `./gradlew assembleRelease`
- 产物：`app/build/outputs/apk/release/app-release.apk`

---

## 10. 里程碑计划（建议 3 周）

### 第 1 周：基础与认证
- 搭建后端与数据库
- 注册/登录/JWT
- Android 登录注册页联通

### 第 2 周：生成主链路
- 文本模型接入（灵感+寓意）
- 生图模型接入
- 作品入库 + 历史列表

### 第 3 周：收藏、稳定性、打包
- 收藏全链路
- 异常处理、超时重试、日志
- Release 签名与 APK 交付

---

## 11. 验收标准

- 能注册、登录、退出并维持会话
- 能成功发起一次完整生成（文本 + 生图）
- 成品页正确展示图片/灵感/寓意
- 历史与收藏数据正确
- 免费额度逻辑正确（次数扣减）
- 服务器部署可公网访问
- 提供可安装的 `app-release.apk`

---

## 12. 风险与预案

- 模型响应慢或失败：
  - 增加超时与重试
  - 失败提示可追踪
- API 限流：
  - 后端队列/频率限制
- 密钥泄露风险：
  - 仅后端持有密钥，严禁写入 APK
- 生成图片存储：
  - 先落本地静态目录，后续迁移 OSS

---

## 13. 下一步执行清单（可直接开工）

1. 创建 `backend/` 与 `android/` 两个子项目。
2. 按本计划建立数据库与 API。
3. 完成 Android 登录注册 + 首页框架。
4. 接通硅基流动文本与生图接口。
5. 打通历史/收藏。
6. 产出 Release APK 并真机安装验收。
