# Netlify + Supabase + 后端部署（按步骤照做）

你当前 Supabase 项目 URL：

- `https://yclkwilangwsmnnpqmwc.supabase.co`

---

## 1) Supabase 侧准备

1. 打开 Supabase 项目：`yclkwilangwsmnnpqmwc`
2. 进入 `Project Settings -> Database`
3. 复制两个连接串：
   - Pooler (6543) -> 用于 `DATABASE_URL`
   - Direct (5432) -> 用于 `DIRECT_URL`
4. 数据库用户名通常是：`postgres.yclkwilangwsmnnpqmwc`
5. 准备数据库密码（你创建项目时设置的）

---

## 2) 本地后端配置

在 `backend/` 目录：

1. 复制模板

```bash
cp .env.example .env
```

2. 编辑 `backend/.env`，只需要填这 4 个值：

- `JWT_SECRET`
- `DATABASE_URL`（替换 `YOUR_DB_PASSWORD`，并把 `REGION` 替换成你控制台里的真实 region）
- `DIRECT_URL`（替换 `YOUR_DB_PASSWORD`）
- `SILICONFLOW_API_KEY`

3. 初始化 Prisma 并迁移：

```bash
npm install
npm run prisma:generate
npx prisma migrate dev --name init_supabase
```

4. 启动后端：

```bash
npm run start:dev
```

5. 健康检查：

`http://127.0.0.1:8080/health` 应返回 `{"ok":true,"db":true}`

---

## 3) 后端线上部署（推荐 Render）

1. Render 新建 `Web Service`
2. 连接仓库：`yys806/yushi2`
3. 设置：
   - Root Directory: `backend`
   - Build Command: `npm install && npm run prisma:generate && npm run build`
   - Start Command: `npm run prisma:migrate:deploy && npm run start`
4. 环境变量填：
   - `PORT=8080`
   - `JWT_SECRET=...`
   - `SUPABASE_URL=https://yclkwilangwsmnnpqmwc.supabase.co`
   - `DATABASE_URL=...`
   - `DIRECT_URL=...`
   - `SILICONFLOW_API_KEY=...`
   - `SILICONFLOW_BASE_URL=https://api.siliconflow.cn/v1`
   - `SILICONFLOW_TEXT_MODEL=Pro/MiniMaxAI/MiniMax-M2.5`
   - `SILICONFLOW_IMAGE_MODEL=Qwen/Qwen-Image`

部署成功后记下后端地址，例如：`https://xxx.onrender.com`

---

## 4) Netlify 前端配置

1. 进入 Netlify 站点设置
2. `Site configuration -> Environment variables`
3. 新增：

`VITE_API_BASE_URL=https://你的后端域名`

4. 触发重新部署（Redeploy）

---

## 5) 你最终必须填写的值清单

1. `JWT_SECRET`
2. `DATABASE_URL`（Supabase Pooler）
3. `DIRECT_URL`（Supabase Direct）
4. `SILICONFLOW_API_KEY`
5. Netlify 的 `VITE_API_BASE_URL`

其他配置都已经在项目里预置好了。
